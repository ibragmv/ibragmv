# ------------------------------------------------------------
# Profile README Repository
# ------------------------------------------------------------
# This project does not require runtime secrets right now.
# Keep real values in `.env`, which stays local and ignored.

# Example extension point:
# GITHUB_USERNAME=ibragmv
